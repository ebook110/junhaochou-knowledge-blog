# Project Memory

- Goal: implement the JunhaoChou Knowledge Blog as a static Chinese Astro site deployable to Ubuntu VPS.
- Baseline: empty Git repository on 2026-08-06; Node 24 and Docker Compose are available. Use `npm.cmd` on this Windows host because PowerShell blocks `npm.ps1`.
- Decisions: public identity is JunhaoChou, GitHub `https://github.com/ebook110`, email `junhaozhou163@gmail.com`; external integrations remain optional environment variables.
- Constraint: no built-in image generation tool is exposed in this session. Do not use API fallback without explicit authorization and a locally configured key.
- Implemented: Astro 7 static site with Tailwind, MDX Content Collections, Pagefind, Shiki, Mermaid, KaTeX, eight Chinese sample articles, category/tag/series routes, SEO/RSS/sitemap/robots, search modal, responsive sidebars, optional Giscus/analytics, Docker/Nginx, CI, documentation and Playwright tests.
- User-level Skills: six `junhao-*` Skills were initialized under `C:\Users\Tom bill\.codex\skills` and validated with `quick_validate.py` after installing local-user PyYAML.
- Verification: `npm.cmd run test` passed on 2026-08-06 (Astro build, 62-file internal link scan, 24 Playwright desktop/mobile tests). Docker Compose config passed; image `junhaochou-knowledge-blog:latest` built and the running loopback container returned HTTP 200 from `/healthz/` with Docker health `healthy`.
- Remaining limitation: generated raster hero and article cover images could not be created because the built-in image tool is unavailable; the site uses a local CSS technical visual treatment that can be replaced with generated assets later. Astro reports non-fatal migration hints for Markdown plugins and a Shiki APDL plaintext fallback; Pagefind reports expected lack of Chinese stemming.
- Frontend refresh (2026-08-07): redesigned the site as a compact engineering-document system. The local `scripts/generate-cover-art.py` uses bundled Pillow to regenerate nine text-free 1600x900 WebP assets under `public/images/covers/`; this removed the previous shared CSS cover placeholder without relying on external image services.
- Frontend interfaces: every article now requires `cover.src`, Chinese `cover.alt`, and optional `cover.focal` in Content Collections. `CoverImage.astro` is the shared image renderer; article pages pass the cover into Open Graph/Twitter metadata and expose a mobile table-of-contents dialog plus reading progress.
- Final verification after refresh: `npm.cmd run format:check`, `lint`, `build`, `test:links`, and `test:e2e` passed (28 Playwright tests); Docker image rebuilt, compose container healthy, and `http://127.0.0.1:8080/healthz/` returned 200. Tailwind dark variants were explicitly bound to `.dark` to prevent mixed light/dark surfaces.
- V1.1 CMS/deployment extension (2026-08-07): added Decap CMS static shell and schema-compatible editorial workflow at `public/admin/`, using the GitHub backend on `main` with explicit repository/Worker placeholders. Added independent Cloudflare Worker OAuth template under `deploy/cloudflare-oauth/`; it uses signed short-lived state, restricted callback `postMessage`, escaped error output and a restrictive callback CSP.
- V1.1 automation/docs: the existing CI workflow now has a serialized deploy job for verified pushes to `main`, using only GitHub Actions secret names, strict SSH host-key verification, a clean VPS worktree, fast-forward-only Git update, Compose rebuild and local health check. Admin/CMS, Cloudflare Access, OAuth, Actions secrets, VPS Deploy Key and troubleshooting docs were added or extended.
- V1.1 verification: `npm.cmd run format:check`, `lint`, targeted Worker `tsc --skipLibCheck`, `check`, `test` (static build, Pagefind, 63-file internal link scan and 32 Playwright tests), `docker compose config`, and `docker compose up --build -d` passed. The rebuilt loopback container is healthy, `/healthz/` and `/admin/` return 200, `robots.txt` disallows `/admin/`, sitemap excludes it, and final image checks confirmed no `/app`, `node_modules` or `package.json`.
- V1.1 external setup (2026-08-07): public GitHub repository `ebook110/junhaochou-knowledge-blog` was initialized and pushed on `main` (initial commit `133af1f`). The Decap OAuth App, workers.dev subdomain and Worker `junhaochou-decap-oauth` exist; Client ID/Secret are Cloudflare Worker secrets and the repository Worker was deployed through Wrangler (latest verified deployment ID `e3b494d8-c075-4573-ac01-04f2517d25ef`). CMS configuration and the Worker correction were published on `main` as commits `9deec7b` and `4965a1b`; `e79f370` records the external setup status. GitHub Actions/VPS secrets await concrete VPS details. Astro check still has baseline deprecation/inline-script hints; they are non-fatal and unrelated to V1.1.
- Production Access update (2026-08-08): Cloudflare Zero Trust application `JunhaoChou CMS Admin` now protects `junhaochou.com/admin/*`. Its Allow policy is limited to `junhaozhou163@gmail.com` and the available identity provider is One-time PIN. The outstanding production prerequisites are the VPS host/initial SSH administrator access, Origin CA installation, proxied DNS records, and GitHub Actions VPS secrets; no deployment credentials are stored in Git.
- Production VPS preparation (2026-08-08): the shared Ubuntu host has an isolated `junhao-deploy` account, public repository worktree at `/opt/junhaochou-blog/app`, a loopback-only blog port `8081`, and an Origin CA certificate held only under a root-owned Nginx certificate directory. Existing AI, network and host-Nginx services remain isolated. Pending: publish the port-aware deployment configuration, add the Actions secret values through GitHub, then build the blog, enable its dedicated Nginx virtual host, validate the Cloudflare proxy chain and exercise Decap OAuth without publishing test content.
